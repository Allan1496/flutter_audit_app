import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
      appBar: AppBar(title: Text('Audit Tracker')),
      body: Center(child: Text('Hello from your Audit App')),
    ),
  ));
}
